import type { InitialData, Context } from './types/initial-data';
import type { PlayerResponse } from './types/player-response';
export declare function TryGetYouTubePage(url: string): Promise<{
    initialData: InitialData;
    playerResponse: PlayerResponse;
    apiToken: string;
    context: Context;
    transcriptMeta: {
        key: string;
        params: string;
        visitorData: string;
        sessionId: string;
        clickTrackingParams: string;
    };
}>;
type page = ReturnType<typeof TryGetYouTubePage>;
export declare const getYouTubePage: (url: string) => Promise<{
    initialData: InitialData;
    playerResponse: PlayerResponse;
    apiToken: string;
    context: Context;
    transcriptMeta: {
        key: string;
        params: string;
        visitorData: string;
        sessionId: string;
        clickTrackingParams: string;
    };
}>;
export type Page = Awaited<page>;
export {};
